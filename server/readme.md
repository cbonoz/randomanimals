# Random Animals Server Code

## Setup
Start the server on the default mongo port using mongod (before starting the nodejs server)

## Access via the mongo shell
mongo
help
show dbs
use random-animals
db.animals.find()
etc..